import React from 'react';
import { Button } from '../button';

export const DropdownCard: React.FC = props => (
  <div className="a-dropdown-card">
    <div className="a-dropdown-card__items" />
    <Button>GO TO CHECKOUT</Button>
  </div>
);
